<%= form_with(model: movie, local: true) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_name %>
    <%= form.text_field :movie_name, id: :movie_movie_name %>
  </div>

  <div class="field">
    <%= form.label :directore %>
    <%= form.text_field :directore, id: :movie_directore %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
